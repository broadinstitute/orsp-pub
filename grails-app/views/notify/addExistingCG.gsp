<p>
    A Sample/Data Cohort record <a href="${issueLink}">${issue.summary}</a> has been added to Project <a href="${values.get("projectLink")}">${values.get("projectSummary")}</a>, and is pending ORSP review and approval. Please note that work with the samples or data should not start until the sample/data cohort has been approved by ORSP and/or the IRB-of-record (as needed).  Approval status is shown in the sample/data cohort tab.
</p>
<p>
    Thank you,
    <br>The ORSP team.
</p>
